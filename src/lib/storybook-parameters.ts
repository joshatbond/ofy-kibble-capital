/** Docs hint for shared `src/components/ui/*` stories — theme comes from the Storybook toolbar. */
export const sharedUiStoryParameters = {
  docs: {
    description: {
      component:
        'Shared primitive — use the **Theme** toolbar to preview under any `data-theme`. Stitch surfaces use **brutal** / **brutal-lg** (offset shadow + ink border).',
    },
  },
} as const
